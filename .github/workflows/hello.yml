name: Hello world!

on:
  push:
    branches:
      - main

jobs:
  print_hello_world:
    runs-on: ubuntu-20.04
    steps:
      - name: print hello world
        run: echo "Hello World!"

      - name: print date
        run: date

      - name: print directory content
        run: ls -l
