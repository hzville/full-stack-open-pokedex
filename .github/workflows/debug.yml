name: debug pipeline

on:
  push:
    branches:
      - main

jobs:
  lint:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v4
      - name: install npm
        uses: ./.github/actions/npm-install
      - name: run eslint
        run: npm run eslint
